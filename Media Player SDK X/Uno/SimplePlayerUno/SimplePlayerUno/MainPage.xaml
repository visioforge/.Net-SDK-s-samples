<Page x:Class="SimplePlayerUno.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vf="using:VisioForge.Core.UI.Uno"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <!-- Video View -->
    <vf:VideoView x:Name="videoView"
                  Grid.Row="0"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  Background="Black" />

    <!-- Controls Panel -->
    <StackPanel Grid.Row="1"
                Background="#1e1e1e"
                Padding="16">

      <!-- Seeking Slider -->
      <Slider x:Name="slSeeking"
              Minimum="0"
              Maximum="100"
              Value="0"
              HorizontalAlignment="Stretch"
              Margin="0,0,0,8"
              ValueChanged="slSeeking_ValueChanged" />

      <!-- Position / Duration Labels -->
      <Grid Margin="0,0,0,8">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0"
                   x:Name="lbPosition"
                   Text="00:00:00"
                   Foreground="White"
                   VerticalAlignment="Center"
                   Margin="5,0,5,0" />

        <TextBlock Grid.Column="2"
                   x:Name="lbDuration"
                   Text="00:00:00"
                   Foreground="White"
                   VerticalAlignment="Center"
                   Margin="5,0,5,0" />
      </Grid>

      <!-- Playback Controls -->
      <Grid Margin="0,0,0,8">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="120" />
        </Grid.ColumnDefinitions>

        <Button x:Name="btPlayStop"
                Grid.Column="0"
                Width="40"
                Height="40"
                Padding="0"
                Margin="0,0,8,0"
                Click="btPlayStop_Click">
          <FontIcon x:Name="playStopIcon" 
                    FontFamily="{ThemeResource SymbolThemeFontFamily}" 
                    Glyph="&#xE768;" 
                    FontSize="20" />
        </Button>

        <TextBox x:Name="txtMediaUrl"
                 Grid.Column="1"
                 PlaceholderText="Enter media URL or file path..."
                 Text="https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/BigBuckBunny.mp4"
                 VerticalAlignment="Center"
                 Margin="0,0,8,0" />

        <FontIcon Grid.Column="2"
                  FontFamily="{ThemeResource SymbolThemeFontFamily}"
                  Glyph="&#xE767;"
                  FontSize="16"
                  Foreground="White"
                  VerticalAlignment="Center"
                  Margin="0,0,4,0" />

        <Slider x:Name="slVolume"
                Grid.Column="3"
                Minimum="0"
                Maximum="100"
                Value="25"
                VerticalAlignment="Center"
                ValueChanged="slVolume_ValueChanged" />

      </Grid>

    </StackPanel>
  </Grid>
</Page>
