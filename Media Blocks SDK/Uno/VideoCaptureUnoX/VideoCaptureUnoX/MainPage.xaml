<Page x:Class="VideoCaptureUnoX.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vf="using:VisioForge.Core.UI.Uno"
      xmlns:toolkit="using:Uno.UI.Toolkit"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <vf:VideoView x:Name="videoView"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      Grid.Row="0"/>

        <StackPanel x:Name="pnMain" Orientation="Vertical" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Background="#1e1e1e" Grid.Row="1"
                    toolkit:VisibleBoundsPadding.PaddingMask="Bottom">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Vertical" Grid.Column="0" HorizontalAlignment="Stretch" Margin="5">
                    <TextBlock Text="CAMERA" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="5" Foreground="White" />
                    <Button x:Name="btCamera" Content="CAMERA" HorizontalAlignment="Stretch" Click="btCamera_Clicked" />
                </StackPanel>

                <StackPanel Orientation="Vertical" Grid.Column="1" HorizontalAlignment="Stretch" Margin="5">
                    <TextBlock Text="MICROPHONE" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="5" Foreground="White" />
                    <Button x:Name="btMic" Content="MICROPHONE" HorizontalAlignment="Stretch" Click="btMic_Clicked" />
                </StackPanel>

                <StackPanel Orientation="Vertical" Grid.Column="2" HorizontalAlignment="Stretch" Margin="5">
                    <TextBlock Text="SPEAKERS" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="5" Foreground="White" />
                    <Button x:Name="btSpeakers" Content="SPEAKERS" HorizontalAlignment="Stretch" Click="btSpeakers_Clicked" />
                </StackPanel>
            </Grid>

            <StackPanel 
                Orientation="Horizontal"
                HorizontalAlignment="Center"
                Margin="5, 0, 5, 0">

                <Button  
                    x:Name="btStartPreview"   
                    Content="PREVIEW" 
                    Margin="5" 
                    Click="btStartPreview_Clicked"
                    MinWidth="100" />

                <Button  
                    x:Name="btStartCapture"   
                    Content="CAPTURE" 
                    Margin="5" 
                    Click="btStartCapture_Clicked"
                    MinWidth="100" />

                <Button  
                    x:Name="btStop"   
                    Content="STOP" 
                    Margin="5" 
                    Click="btStop_Clicked" 
                    MinWidth="100" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
