<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Simple_Player_MB_MAUI.MainPage"
             xmlns:my="clr-namespace:VisioForge.Core.UI.MAUI;assembly=VisioForge.Core.UI.MAUI"
             xmlns:skins="clr-namespace:VisioForge.Core.UI.MAUI.Skins;assembly=VisioForge.Core.UI.MAUI">

    <Grid>
        <!--WidthRequest="1280" HeightRequest="720"-->
        <Grid RowSpacing="0" RowDefinitions="*,Auto">

            <my:VideoView Grid.Row="0"
                          HorizontalOptions="FillAndExpand"
                          VerticalOptions="FillAndExpand"
                          x:Name="videoView"
                          Background="Black">
                <my:VideoView.GestureRecognizers>
                    <PanGestureRecognizer PanUpdated="OnPanUpdated" />
                </my:VideoView.GestureRecognizers>
            </my:VideoView>

            <StackLayout Grid.Row="1" x:Name="pnMain" Orientation="Vertical" HorizontalOptions="Fill"
                         Background="#1e1e1e">
                <Slider
                    x:Name="slSeeking"
                    HorizontalOptions="Fill"
                    Margin="16, 16, 16, 0"
                    VerticalOptions="Center"
                    ValueChanged="slSeeking_ValueChanged" />

                <Grid Margin="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="auto" />
                    </Grid.ColumnDefinitions>

                    <Label
                        Grid.Column="0"
                        Text="00:00:00"
                        x:Name="lbPosition"
                        VerticalOptions="Center"
                        HorizontalOptions="Start"
                        TextColor="White"
                        Margin="5, 0, 5, 0" />

                    <Label
                        Grid.Column="1"
                        Text="FILENAME"
                        x:Name="lbFilename"
                        VerticalOptions="Center"
                        HorizontalOptions="Center"
                        TextColor="White"
                        Margin="0"
                        IsVisible="false" />

                    <Label
                        Grid.Column="2"
                        Text="00:00:00"
                        x:Name="lbDuration"
                        VerticalOptions="Center"
                        HorizontalOptions="End"
                        TextColor="White"
                        Margin="5, 0, 5, 0" />
                </Grid>

                <!-- Playback buttons fill the full width so nothing overflows on a narrow phone -->
                <Grid ColumnDefinitions="*,*,*" ColumnSpacing="8" Margin="16,4,16,0">
                    <Button
                        Grid.Column="0"
                        x:Name="btOpen"
                        Text="OPEN FILE"
                        HorizontalOptions="Fill"
                        Clicked="btOpen_Clicked" />

                    <Button
                        Grid.Column="1"
                        x:Name="btPlayPause"
                        Text="PLAY"
                        HorizontalOptions="Fill"
                        Clicked="btPlayPause_Clicked" />

                    <Button
                        Grid.Column="2"
                        x:Name="btStop"
                        Text="STOP"
                        HorizontalOptions="Fill"
                        Clicked="btStop_Clicked" />
                </Grid>

                <!-- Volume label + slider on their own full-width row -->
                <Grid ColumnDefinitions="Auto,*" ColumnSpacing="10" Margin="16,4,16,12">
                    <Label
                        Grid.Column="0"
                        Text="Volume"
                        TextColor="White"
                        VerticalOptions="Center" />

                    <Slider
                        Grid.Column="1"
                        x:Name="slVolume"
                        HorizontalOptions="Fill"
                        Minimum="0"
                        Maximum="100"
                        Value="25"
                        VerticalOptions="Center"
                        ValueChanged="slVolume_ValueChanged" />
                </Grid>
            </StackLayout>
        </Grid>
    </Grid>
</ContentPage>