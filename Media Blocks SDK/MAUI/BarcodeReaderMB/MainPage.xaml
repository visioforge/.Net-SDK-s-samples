<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BarcodeReaderMB.MainPage"
             xmlns:my="clr-namespace:VisioForge.Core.UI.MAUI;assembly=VisioForge.Core.UI.MAUI">

    <Grid>
        <my:VideoView 
                x:Name="videoView"
                HorizontalOptions="FillAndExpand"
                VerticalOptions="FillAndExpand" />
                   
        <StackLayout x:Name="pnMain" Orientation="Vertical" HorizontalOptions="Fill" VerticalOptions="End" Background="#1e1e1e">

            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                
                <!-- Detection Status -->
                <StackLayout Grid.Row="0" Orientation="Vertical" Padding="10" Background="#2d2d2d" Margin="0,0,0,10">
                    <Label Text="LAST DETECTED BARCODE" HorizontalOptions="Center" FontSize="12" TextColor="#888" />
                    <Label x:Name="lbBarcodeType" Text="Type: -" HorizontalOptions="Center" FontSize="14" TextColor="White" Margin="0,5,0,0" />
                    <Label x:Name="lbBarcodeValue" Text="Value: -" HorizontalOptions="Center" FontSize="16" FontAttributes="Bold" TextColor="#4CAF50" Margin="0,2,0,0" />
                    <Label x:Name="lbDetectionCount" Text="Detected: 0" HorizontalOptions="Center" FontSize="12" TextColor="#888" Margin="0,5,0,0" />
                </StackLayout>
                
                <!-- Controls -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackLayout Orientation="Vertical" Grid.Column="0" HorizontalOptions="FillAndExpand" Margin="5">
                        <Label Text="CAMERA" VerticalOptions="Center" HorizontalOptions="Center" Margin="5" TextColor="White" FontSize="12" />
                        <Button x:Name="btCamera" Text="SELECT CAMERA" Clicked="btCamera_Clicked" />
                    </StackLayout>

                    <StackLayout Orientation="Vertical" Grid.Column="1" HorizontalOptions="FillAndExpand" Margin="5">
                        <Label Text="CONTROL" VerticalOptions="Center" HorizontalOptions="Center" Margin="5" TextColor="White" FontSize="12" />
                        <Button x:Name="btStartStop" Text="START" Clicked="btStartStop_Clicked" BackgroundColor="#4CAF50" />
                    </StackLayout>
                </Grid>
            </Grid>

        </StackLayout>
    </Grid>

</ContentPage>
