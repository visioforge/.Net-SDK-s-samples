<Window x:Class="File_Encryptor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:File_Encryptor"
        mc:Ignorable="d"
        Title="Video Encryptor Demo (WPF)" Height="560" Width="660"
        Loaded="Window_Loaded" Closing="Window_Closing" Icon="visioforge_main_icon.ico">
    <Grid>
        <StackPanel Orientation="Vertical" Margin="10,10,10,0" HorizontalAlignment="Stretch" VerticalAlignment="Top">
            <TextBlock Text="File will be encrypted without reencoding for H264 video and AAC audio, otherwise file will be reencoded." TextWrapping="Wrap" Margin="0,0,0,10" />

            <TextBlock Text="Input file" />
            <StackPanel Orientation="Horizontal">
                <TextBox x:Name="edInputFile" Width="570" Margin="0,0,10,0" />
                <Button Content="..." Click="btInputFile_Click" Width="25" />
            </StackPanel>

            <TextBlock Text="Output file" Margin="0,10,0,0" />
            <StackPanel Orientation="Horizontal">
                <TextBox x:Name="edOutputFile" Width="570" Margin="0,0,10,0" />
                <Button Content="..." Click="btOutputFile_Click" Width="25" />
            </StackPanel>

            <Grid Margin="0,15,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="Log" />
                    <TextBox x:Name="edLog" Height="250" IsReadOnly="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,5,0,0" />
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="Key" />
                    <TextBox x:Name="edKey" Text="somepassword" Margin="0,5,0,0" />
                    <TextBlock Text="Also, you can use the binary key or file." Margin="0,10,0,0" TextWrapping="Wrap" />
                    <CheckBox x:Name="cbForceRecompress" Content="Force recompress" Margin="0,15,0,0" />
                </StackPanel>
            </Grid>

            <StackPanel Orientation="Horizontal" Margin="0,15,0,0" HorizontalAlignment="Right">
                <ProgressBar x:Name="pbProgress" Width="200" Height="25" Margin="0,0,15,0" />
                <Button x:Name="btStart" Content="Start" Click="btStart_Click" Width="80" Height="30" />
                <Button x:Name="btStop" Content="Stop" Click="btStop_Click" Width="80" Height="30" Margin="10,0,0,0" IsEnabled="False" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
