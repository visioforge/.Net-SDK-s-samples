<Window x:Class="EncoderConcurrencyTestX.WebcamConfigDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configure webcam source"
        Width="520" Height="280"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Loaded="Window_Loaded">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Camera"
                   VerticalAlignment="Center" Margin="0,4"/>
        <ComboBox  Grid.Row="0" Grid.Column="1" x:Name="cbDevice"
                   SelectionChanged="CbDevice_SelectionChanged" Margin="0,4"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Format"
                   VerticalAlignment="Center" Margin="0,4"/>
        <ComboBox  Grid.Row="1" Grid.Column="1" x:Name="cbFormat"
                   SelectionChanged="CbFormat_SelectionChanged" Margin="0,4"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Frame rate"
                   VerticalAlignment="Center" Margin="0,4"/>
        <ComboBox  Grid.Row="2" Grid.Column="1" x:Name="cbFrameRate"
                   Margin="0,4"/>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="btOk" Content="OK" Width="90" IsDefault="True"
                    Click="BtOk_Click" Margin="0,0,8,0"/>
            <Button Content="Cancel" Width="90" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
