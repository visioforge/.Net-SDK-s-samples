<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="PhotoCapture.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="using:PhotoCapture"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Photo Capture Sample">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Camera Selection -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <TextBlock Text="Camera:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox x:Name="CameraComboBox" Width="300" SelectionChanged="CameraComboBox_SelectionChanged"/>
            <TextBlock Text="Resolution:" VerticalAlignment="Center" Margin="20,0,10,0"/>
            <ComboBox x:Name="ResolutionComboBox" Width="200" SelectionChanged="ResolutionComboBox_SelectionChanged"/>
        </StackPanel>

        <!-- Camera Preview -->
        <Border Grid.Row="1" Background="Black" Margin="10">
            <Grid>
                <!-- Image for camera preview -->
                <Image x:Name="PreviewImage" Stretch="Uniform"/>
                
                <!-- Status message -->
                <TextBlock x:Name="StatusTextBlock" 
                          HorizontalAlignment="Center" 
                          VerticalAlignment="Center"
                          Foreground="White"
                          FontSize="18"
                          Text="Click 'Start Camera' to begin"/>
            </Grid>
        </Border>

        <!-- Controls -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button x:Name="StartCameraButton" Content="Start Camera" Click="StartCameraButton_Click" Margin="5"/>
            <Button x:Name="StopCameraButton" Content="Stop Camera" Click="StopCameraButton_Click" IsEnabled="False" Margin="5"/>
            <Button x:Name="TakePhotoButton" Content="Take Photo" Click="TakePhotoButton_Click" IsEnabled="False" Margin="5"/>
            
            <StackPanel Orientation="Horizontal" Margin="20,0,0,0">
                <TextBlock Text="Flash:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox x:Name="FlashComboBox" Width="100" SelectionChanged="FlashComboBox_SelectionChanged">
                    <ComboBoxItem Content="Auto" Tag="Auto"/>
                    <ComboBoxItem Content="On" Tag="On"/>
                    <ComboBoxItem Content="Off" Tag="Off" IsSelected="True"/>
                </ComboBox>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" Margin="20,0,0,0">
                <TextBlock Text="Zoom:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <Slider x:Name="ZoomSlider" Width="150" Minimum="1" Maximum="5" Value="1" 
                        ValueChanged="ZoomSlider_ValueChanged" IsEnabled="False"/>
                <TextBlock x:Name="ZoomValueTextBlock" Text="1.0x" VerticalAlignment="Center" Margin="5,0,0,0"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>